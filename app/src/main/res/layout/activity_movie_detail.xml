<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/main_view"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.movie_detail.MovieDetailActivity">

    <FrameLayout
        android:id="@+id/main_container_movie_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbar"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/bnv_movie_detail_nav"
        />

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/bnv_movie_detail_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="0dp"
        android:layout_marginStart="0dp"
        android:background="?android:attr/windowBackground"
        app:menu="@menu/menu_movie_detail_navigation"
        android:layout_alignParentBottom="true"
        />

</RelativeLayout>